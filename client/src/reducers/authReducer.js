function authReducer(state = {}, action) {
    
    // uncomment to see the action recieved
    // console.log(action);
    
    switch (action.type) {
        default:
            return state;
    }
}

export default authReducer;